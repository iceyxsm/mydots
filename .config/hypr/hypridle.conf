# Hypridle Configuration - Cyberpunk Auto-lock

# General settings
general {
    lock_cmd = hyprlock
    unlock_cmd = 
    before_sleep_cmd = hyprlock
    after_sleep_cmd = 
}

# Listen for lock signals
listener {
    timeout = 600
    on-timeout = hyprlock
    on-resume = 
}

# Dim screen before locking (optional)
listener {
    timeout = 540
    on-timeout = brightnessctl -s set 10
    on-resume = brightnessctl -r
}
